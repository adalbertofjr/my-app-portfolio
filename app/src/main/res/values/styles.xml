<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Button Style. -->
    <style name="Button.AppTheme" parent="Base.TextAppearance.AppCompat.Button">
        <item name="android:layout_marginTop">@dimen/activity_vertical_space_widgets</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@color/colorAccent</item>
        <item name="android:layout_width">@dimen/activity_main_button_width</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>
